name: Release

on:
    push:
        tags:
            - "v*"
    workflow_dispatch:
        inputs:
            version:
                description: "Version to release (e.g., v1.0.0)"
                required: true
                type: string

permissions:
    contents: write
    packages: write
    attestations: write
    id-token: write
    security-events: write

jobs:
    release:
        name: Build and Release
        uses: falkcorp/github-common/.github/workflows/reusable-release.yml@bfb912c850effd1ae9aded26d14856a538d3e5fc # v1.10.7
        with:
            build-target: "go"
            go-enabled: true
        secrets: inherit
