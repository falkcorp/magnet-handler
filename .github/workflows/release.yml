name: Release

on:
    push:
        tags:
            - "v*"
    workflow_dispatch:
        inputs:
            version:
                description: "Version to release (e.g., v1.0.0)"
                required: true
                type: string

permissions:
    contents: write
    packages: write
    attestations: write
    id-token: write
    security-events: write

jobs:
    release:
        name: Build and Release
        uses: jdfalk/ghcommon/.github/workflows/reusable-release.yml@7d1de69939871d95024c649cf5214b137bb64fb7 # v1.10.5
        with:
            build-target: "go"
            go-enabled: true
        secrets: inherit
